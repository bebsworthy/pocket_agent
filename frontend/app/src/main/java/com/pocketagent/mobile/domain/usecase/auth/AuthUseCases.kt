package com.pocketagent.mobile.domain.usecase.auth

import kotlinx.coroutines.CoroutineDispatcher

class AuthenticateUserUseCase(private val dispatcher: CoroutineDispatcher) {
    // Implementation will be added in Task 3.2: Create App Launch Authentication Manager
}

class ValidateBiometricUseCase(private val dispatcher: CoroutineDispatcher) {
    // Implementation will be added in Task 3.2: Create App Launch Authentication Manager
}